# Copy to .env to enable cloud backup of stats.
# Use the ApiUrl output from `cdk deploy` (no trailing slash).
# Example:
# VITE_API_BASE_URL=https://abc123xyz.execute-api.us-east-2.amazonaws.com
VITE_API_BASE_URL=
